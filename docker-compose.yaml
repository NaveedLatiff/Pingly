version: '3.8'
services:
  backend:
    build: ./server
    ports:
      - "3003:3003"
    restart: always
  frontend:
    build: ./client
    args:
        - NEXT_PUBLIC_BACKEND_URL=${NEXT_PUBLIC_BACKEND_URL}
    container_name: pingly_frontend_1
    ports:
      - "3000:3000"
    restart: always